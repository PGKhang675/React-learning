// React hook = Special functions that allow functional components to use React featrues 
//              without writing class components
//              (useState(), useEffect(), useContext(), userReducer(), useCallback(), ...)

// useState() = A React hook that allows a creation of a stateful variable and a Setter function
//              to update its value in the virtual DOM.
//              [name, setName]

import MyComponent from "./MyComponent";

function App(){
  return(
    <MyComponent></MyComponent>
  );
}

export default App